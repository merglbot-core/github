name: Deploy to Cloud Run (WIF)

on:
  workflow_call:
    inputs:
      service_name:
        description: 'Cloud Run service name'
        required: true
        type: string
      region:
        description: 'GCP region'
        required: false
        type: string
        default: 'europe-west1'
      image_name:
        description: 'Container image name (without registry prefix)'
        required: true
        type: string
      deploy_project_id:
        description: 'Cloud Run deploy project ID (defaults to secret GCP_PROJECT_ID)'
        required: false
        type: string
        default: ''
      artifact_project_id:
        description: 'Artifact Registry project ID (defaults to deploy project)'
        required: false
        type: string
        default: ''
      artifact_location:
        description: 'Artifact Registry location (defaults to Cloud Run region)'
        required: false
        type: string
        default: ''
      artifact_repository:
        description: 'Artifact Registry repository name'
        required: false
        type: string
        default: 'containers'
      dockerfile:
        description: 'Path to Dockerfile'
        required: false
        type: string
        default: 'Dockerfile'
      environment_name:
        description: 'Optional GitHub Environment name for the deploy job'
        required: false
        type: string
        default: ''
      environment_url:
        description: 'Optional GitHub Environment URL for the deploy job'
        required: false
        type: string
        default: ''
      runtime_service_account:
        description: 'Optional runtime service account email for Cloud Run'
        required: false
        type: string
        default: ''
      wif_provider:
        description: 'Optional WIF provider when caller stores auth metadata in vars'
        required: false
        type: string
        default: ''
      wif_service_account:
        description: 'Optional WIF service account when caller stores auth metadata in vars'
        required: false
        type: string
        default: ''
      ingress:
        description: 'Cloud Run ingress setting'
        required: false
        type: string
        default: ''
      memory:
        description: 'Cloud Run memory allocation'
        required: false
        type: string
        default: '512Mi'
      cpu:
        description: 'Cloud Run CPU allocation'
        required: false
        type: string
        default: '1'
      timeout:
        description: 'Cloud Run request timeout in seconds'
        required: false
        type: number
        default: 300
      port:
        description: 'Cloud Run container port'
        required: false
        type: number
        default: 8080
      allow_unauthenticated:
        description: 'Allow unauthenticated access to the deployed service'
        required: false
        type: boolean
        default: true
      environment:
        description: 'Environment (staging/production)'
        required: false
        type: string
        default: 'staging'
      min_instances:
        description: 'Minimum instances'
        required: false
        type: number
        default: 0
      max_instances:
        description: 'Maximum instances'
        required: false
        type: number
        default: 10
    secrets:
      WIF_PROVIDER:
        description: 'Workload Identity Federation provider'
        required: false
      WIF_SERVICE_ACCOUNT:
        description: 'Service account email for WIF'
        required: false
      GCP_PROJECT_ID:
        description: 'GCP Project ID'
        required: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30  # Build, scan, and deploy usually <15m; buffer for registry latency
    environment:
      name: ${{ inputs.environment_name }}
      url: ${{ inputs.environment_url }}
    permissions:
      contents: read
      id-token: write  # Required for WIF OIDC token
      security-events: write  # Needed for SARIF upload

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Authenticate to GCP (WIF)
        id: auth
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093  # v3.0.0
        with:
          workload_identity_provider: ${{ inputs.wif_provider || secrets.WIF_PROVIDER }}
          service_account: ${{ inputs.wif_service_account || secrets.WIF_SERVICE_ACCOUNT }}
          token_format: 'access_token'

      - name: Resolve deployment parameters
        shell: bash
        env:
          INPUT_DEPLOY_PROJECT_ID: ${{ inputs.deploy_project_id }}
          INPUT_ARTIFACT_PROJECT_ID: ${{ inputs.artifact_project_id }}
          INPUT_ARTIFACT_LOCATION: ${{ inputs.artifact_location }}
          INPUT_ARTIFACT_REPOSITORY: ${{ inputs.artifact_repository }}
          INPUT_REGION: ${{ inputs.region }}
          SECRET_GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
        run: |
          set -euo pipefail

          DEPLOY_PROJECT_ID="${INPUT_DEPLOY_PROJECT_ID:-$SECRET_GCP_PROJECT_ID}"
          ARTIFACT_PROJECT_ID="${INPUT_ARTIFACT_PROJECT_ID:-$DEPLOY_PROJECT_ID}"
          ARTIFACT_LOCATION="${INPUT_ARTIFACT_LOCATION:-$INPUT_REGION}"
          ARTIFACT_REPOSITORY="${INPUT_ARTIFACT_REPOSITORY:-containers}"

          if [ -z "$DEPLOY_PROJECT_ID" ]; then
            echo "::error::deploy_project_id is empty and secret GCP_PROJECT_ID is not set"
            exit 1
          fi

          if [ -z "$ARTIFACT_PROJECT_ID" ]; then
            echo "::error::artifact_project_id could not be resolved"
            exit 1
          fi

          if [ -z "$ARTIFACT_LOCATION" ]; then
            echo "::error::artifact_location could not be resolved"
            exit 1
          fi

          if [ -z "$ARTIFACT_REPOSITORY" ]; then
            echo "::error::artifact_repository could not be resolved"
            exit 1
          fi

          {
            printf 'DEPLOY_PROJECT_ID=%s\n' "$DEPLOY_PROJECT_ID"
            printf 'ARTIFACT_PROJECT_ID=%s\n' "$ARTIFACT_PROJECT_ID"
            printf 'ARTIFACT_LOCATION=%s\n' "$ARTIFACT_LOCATION"
            printf 'ARTIFACT_REPOSITORY=%s\n' "$ARTIFACT_REPOSITORY"
          } >> "$GITHUB_ENV"

      - name: Setup gcloud CLI
        uses: google-github-actions/setup-gcloud@aa5489c8933f4cc7a4f7d45035b3b1440c9c10db  # v3.0.1
        with:
          project_id: ${{ env.DEPLOY_PROJECT_ID }}

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker "${ARTIFACT_LOCATION}-docker.pkg.dev" --quiet

      - name: Build Container Image
        run: |
          IMAGE="${ARTIFACT_LOCATION}-docker.pkg.dev/${ARTIFACT_PROJECT_ID}/${ARTIFACT_REPOSITORY}/${{ inputs.image_name }}:${{ github.sha }}"
          docker build -t "$IMAGE" -f "${{ inputs.dockerfile }}" .
          docker push "$IMAGE"
          echo "IMAGE=$IMAGE" >> "$GITHUB_ENV"

      # Container Security: Trivy scan with SARIF upload
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@e368e328979b113139d6f9068e03accaed98a518  # v0.34.1
        continue-on-error: true  # Allow deploy while we tune blocking thresholds
        with:
          image-ref: ${{ env.IMAGE }}
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL'
          exit-code: '0'         # Do not fail deploy on scan; surfaced via SARIF
          ignore-unfixed: true

      - name: Upload Trivy results
        uses: github/codeql-action/upload-sarif@89a39a4e59826350b863aa6b6252a07ad50cf83e  # v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: 'trivy-results.sarif'

      - name: Deploy to Cloud Run
        env:
          INPUT_TIMEOUT: ${{ inputs.timeout }}
          INPUT_PORT: ${{ inputs.port }}
        run: |
          set -euo pipefail

          deploy_args=(
            "${{ inputs.service_name }}"
            "--image" "${IMAGE}"
            "--region" "${{ inputs.region }}"
            "--project" "${DEPLOY_PROJECT_ID}"
            "--platform" "managed"
            "--min-instances" "${{ inputs.min_instances }}"
            "--max-instances" "${{ inputs.max_instances }}"
            "--set-env-vars=ENVIRONMENT=${{ inputs.environment }}"
            "--quiet"
          )

          if [ -n "${{ inputs.runtime_service_account }}" ]; then
            deploy_args+=("--service-account" "${{ inputs.runtime_service_account }}")
          fi

          if [ -n "${{ inputs.ingress }}" ]; then
            deploy_args+=("--ingress" "${{ inputs.ingress }}")
          fi

          if [ -n "${{ inputs.memory }}" ]; then
            deploy_args+=("--memory" "${{ inputs.memory }}")
          fi

          if [ -n "${{ inputs.cpu }}" ]; then
            deploy_args+=("--cpu" "${{ inputs.cpu }}")
          fi

          if [ "${INPUT_TIMEOUT}" -gt 0 ]; then
            deploy_args+=("--timeout" "${INPUT_TIMEOUT}")
          fi

          if [ "${INPUT_PORT}" -gt 0 ]; then
            deploy_args+=("--port" "${INPUT_PORT}")
          fi

          if [ "${{ inputs.allow_unauthenticated }}" = "true" ]; then
            deploy_args+=("--allow-unauthenticated")
          else
            deploy_args+=("--no-allow-unauthenticated")
          fi

          gcloud run deploy "${deploy_args[@]}"

      - name: Get Service URL
        id: url
        run: |
          URL=$(gcloud run services describe "${{ inputs.service_name }}" \
            --region "${{ inputs.region }}" \
            --project "${DEPLOY_PROJECT_ID}" \
            --format 'value(status.url)')
          echo "url=$URL" >> "$GITHUB_OUTPUT"
          echo "🚀 Service deployed to: $URL"

      - name: Health Check
        run: |
          echo "Performing health check..."
          for i in {1..5}; do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" "${{ steps.url.outputs.url }}/health" || echo "000")
            if [ "$STATUS" = "200" ]; then
              echo "✅ Health check passed!"
              exit 0
            fi
            echo "Attempt $i: Status $STATUS, retrying..."
            sleep 10
          done
          echo "⚠️ Health check did not pass (may be expected for some services)"
